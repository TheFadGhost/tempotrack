{
  "name": "tempotrack",
  "version": "0.1.0",
  "description": "Local-first time tracker combining a pomodoro timer with honest analytics.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^7.1.0",
    "vitest": "^3.2.0"
  }
}
